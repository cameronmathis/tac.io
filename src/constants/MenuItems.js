import { HOME, PAGE_NOT_FOUND } from "./Pages";

const Home = {
  text: "Home",
  path: HOME.path,
};

const Other = {
  text: "Other",
  path: PAGE_NOT_FOUND.path,
};

export const MENU_ITEMS = [Home, Other];
